"""Preview dataset import — a removable demonstration catalog.

Separate from client bootstrap (`app.instance`) and from the generic demo seed
(`scripts.seed`). A preview dataset exists so a client can see their storefront with
plausible content before they have supplied a real catalog, and can then be removed in
one command without leaving anything behind.
"""
